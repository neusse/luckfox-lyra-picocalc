# Weather Icon Assets

This directory intentionally contains only BMP weather icons reused by the
Linux PicoCalc weather app.

It does not include the original CircuitPython app sources or `secrets.py`.
Keep Wi-Fi credentials and OpenWeather API keys outside git.
