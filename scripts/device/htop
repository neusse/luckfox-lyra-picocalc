#!/bin/sh
# Wrapper for the Debian ARMHF htop binary installed on the Buildroot PicoCalc.
export LD_LIBRARY_PATH="/usr/local/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

if [ -z "${TERM:-}" ] || [ "$TERM" = "dumb" ]; then
    export TERM=linux
fi

exec /usr/bin/htop.real "$@"
